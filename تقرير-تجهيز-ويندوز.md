# تقرير تجهيز برنامج Windows

## النتيجة النهائية

تم دمج جميع الملفات، بما فيها ملف `pubspec.yaml` الأخير المرفق، في مشروع Flutter كامل. تم إجراء تحقق فعلي وليس شكليًا:

| الاختبار | النتيجة |
|---|---|
| حل الاعتمادات عبر `flutter pub get` باستخدام Flutter 3.35.4 | نجح، وتم تنزيل 114 حزمة |
| التحليل البرمجي عبر `flutter analyze` | نجح: `No issues found!` |
| ترجمة التطبيق في بيئة Linux مؤقتة | نجحت، وتم إنتاج ملف التطبيق |
| محاولة `flutter build windows --release` | مرفوضة من Flutter لأن المضيف Linux |

## ما تم تجهيزه

- مصدر التطبيق في `lib/`، ويحتوي على 31 ملف Dart.
- الأصول والصور والخطوط وملف البيانات في `assets/`.
- ملفات منصة Windows في `windows/`.
- سكربت البناء `installer/build_windows.bat`.
- سكربت Inno Setup في `installer/windows_setup.iss`.
- ملف `pubspec.yaml` المرفق من المستخدم.
- ملف `pubspec.lock` الناتج عن حل الاعتمادات الفعلي.
- سير عمل GitHub Actions في `.github/workflows/build-windows.yml` لبناء EXE وSetup تلقائيًا على Windows.

## سبب عدم تسليم EXE مباشرة

تمت محاولة تنفيذ الأمر الحقيقي:

```text
flutter build windows --release
```

ورد Flutter بالرسالة:

```text
"build windows" only supported on Windows hosts.
```

أي أن المشكلة ليست في المشروع أو الاعتمادات أو الكود؛ إنها قيد رسمي في Flutter يمنع إخراج Windows من نظام Linux. تم تجاوز ذلك عمليًا بإضافة سير عمل يبني المشروع على خادم Windows تلقائيًا.

## الطريقة الأولى: البناء على جهاز Windows

المتطلبات:

- Windows 10 أو Windows 11.
- Flutter 3.35.4.
- Visual Studio 2022 مع مكوّن **Desktop development with C++**.
- Inno Setup 6 اختياريًا لإنشاء ملف التنصيب.

الأوامر:

```bat
cd academic_inquiry
flutter config --enable-windows-desktop
flutter pub get
flutter analyze
flutter build windows --release
```

أو شغّل:

```bat
installer\\build_windows.bat
```

ينتج التطبيق المحمول في:

```text
build\\windows\\x64\\runner\\Release\\AcademicInquiry.exe
```

يجب توزيع مجلد `Release` كاملًا، وليس ملف EXE منفردًا، لأنه يعتمد على ملفات DLL ومجلد `data`.

لإنشاء ملف Setup:

```bat
ISCC.exe installer\\windows_setup.iss
```

ينتج الملف:

```text
installer\\Output\\AcademicInquiry-Setup-2.0.0-win64.exe
```

## الطريقة الثانية: البناء الآلي عبر GitHub Actions

1. أنشئ مستودعًا جديدًا على GitHub.
2. ارفع محتويات الأرشيف النهائي إلى المستودع.
3. افتح تبويب **Actions**.
4. اختر سير العمل **Build Windows EXE**.
5. اضغط **Run workflow**.
6. بعد انتهاء البناء، حمّل:
   - `AcademicInquiry-Windows-Portable` للتطبيق المحمول.
   - `AcademicInquiry-Windows-Installer` لملف Setup.

يمكن أيضًا تشغيل البناء تلقائيًا عند دفع وسم يبدأ بـ `v` مثل `v2.0.0`.

## ملاحظة تشغيلية

في Windows يستخدم التطبيق قارئ باركود USB أو الإدخال اليدوي. لا تعتمد نسخة سطح المكتب على كاميرا ML Kit.
